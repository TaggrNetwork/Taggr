[package]
name = "canisters"
version = "0.1.0"
edition = "2018"

[lib]
path = "lib.rs"

[dependencies]
candid = "0.8"
ic-cdk = { version = "0.6.8", features = ["timers"] }
ic-ledger-types = "0.2.0"
serde = { version = "1.0", features = ["derive"] }

[features]
dev = []
