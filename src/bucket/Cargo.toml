[package]
name = "bucket"
version = "0.1.0"
edition = "2018"

[lib]
crate-type = ["cdylib"]

[dependencies]
ic-cdk = "0.10.0"
candid = "0.9.0"

[features]
dev = []

[profile.release]
lto = true
opt-level = 2
